#include<bits/stdc++.h>
using namespace std;
string sumstring(string s1,string s2){
	if(s1.size()>s2.size())	swap(s1,s2);
	int n1=s1.size(),n2=s2.size();
	int d=n2-n1,carry=0;
	string s="";
	for(int i=n1-1;i>=0;i--){
		int sum=(s1[i]-'0')+(s2[i+d]-'0')+carry;
		s.push_back(sum%10+'0');
		carry=sum/10;
	}
	for(int i=d-1;i>=0;i--){
		int sum=(s2[i]-'0')+carry;
		s.push_back(sum%10+'0');
		carry=sum/10;
	}
	if(carry>0)	s.push_back(carry+'0');
	reverse(s.begin(),s.end());
	return s;
}
string mulstring(string s1,string s2){ 
	int n=s1.size(),m=s2.size();
	if(n==0||m==0)	return "0";
	vector<int> res(n+m,0);
	for(int i=n-1;i>=0;i--){
		for(int j=m-1;j>=0;j--){
			int mul=(s1[i]-'0')*(s2[j]-'0');
			int sum=mul+res[i+j+1];
			res[i+j+1]=sum%10;
			res[i+j]+=sum/10;		//carry;
		}
	}
	string s="";
	int i=0;
	while(i<res.size()&&res[i]==0)	i++;
	for(int j=i;j<res.size();j++)	s+=res[j]+'0';
	return s;
}
int main(){
	int t;
	cin>>t;
	cin.ignore();
	while(t--){
		string s;
		getline(cin,s);
		vector<string> a;
		stringstream ss(s);
		string word="";
		while(ss>>word){
			a.push_back(word);
		}
		if(a[1]=="+"){
			cout<<sumstring(a[0],a[2])<<endl;
		}
		else{
			cout<<mulstring(a[0],a[2])<<endl;
		}
	}
	return 0;
}
