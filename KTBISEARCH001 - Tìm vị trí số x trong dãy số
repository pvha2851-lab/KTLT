#include<bits/stdc++.h>
using namespace std;
int binarysearch(int a[],int l,int r,int x){
	int pos=0;
	while(l<=r){
		int m=(l+r)/2;
		if(a[m]==x){
			pos=m;
			r=m-1;
		}
		else if(a[m]<x){
			l=m+1;
		}
		else{
			r=m-1;
		}
	}
	return pos;
}
int main(){
	int n,m;
	cin>>n>>m;
	int a[n+1],b[m+1];
	for(int i=1;i<=n;i++){
		cin>>a[i];
	}
	for(int i=1;i<=m;i++){
		cin>>b[i];
	}
	for(int i=1;i<=m;i++){
		cout<<binarysearch(a,1,n,b[i])<<" ";
	}
	return 0;
}
