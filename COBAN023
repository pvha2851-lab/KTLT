#include<bits/stdc++.h>
#define ll long long
using namespace std;
int main(){
	int t;
	cin>>t;
	while(t--){
		ll n,p;
		cin>>n>>p;
		ll power=p; 
		ll res=0; 
		while(n>=power){
			res=res+n/power;
			power=power*p; 
		} 
		cout<<res<<endl; 
	}
	return 0;
}
